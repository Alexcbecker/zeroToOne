(function() {
    'use strict';

    let params = getUrlParameters();

    if (!params['id'] || !!params['id']) {
        window.alert('ID não informado!');
    }
    else {
        let usuarioObj = 
    }
})();